# ============================================================
# KorMedAI - 한국형 의료 AI 시스템 의존성
# ============================================================

# ── LangChain (멀티 에이전트 프레임워크) ──
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# ── 벡터 DB (RAG) ──
chromadb>=0.5.0
faiss-cpu>=1.8.0

# ── 임베딩 모델 ──
sentence-transformers>=3.0.0

# ── LLM 백엔드 ──
# Transformers (MedGemma 비전 모델)
transformers>=4.45.0
torch>=2.4.0
accelerate>=0.34.0

# Ollama (Qwen3-8B)
# → ollama CLI 별도 설치 필요

# vLLM (HARI-Q3-14B) - Linux/CUDA only
# vllm>=0.6.0  # Mac에서는 주석 처리

# MLX (Apple Silicon 최적화 - Mac Mini용)
# mlx-lm>=0.18.0  # Mac에서만 설치

# ── API 서버 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# ── 문서 로더 ──
pypdf>=4.0.0
beautifulsoup4>=4.12.0

# ── HuggingFace 데이터셋 ──
datasets>=3.0.0

# ── 이미지 처리 ──
Pillow>=10.0.0

# ── HTTP 클라이언트 ──
httpx>=0.27.0

# ── 유틸리티 ──
python-dotenv>=1.0.0
